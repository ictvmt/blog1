<%= link_to gravatar_for(current_user, size: 52), current_user %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "Ho so cua toi", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "Entries") %>
</span>